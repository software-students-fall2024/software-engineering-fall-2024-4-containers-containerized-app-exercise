[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pylint = "*"
black = "*"
pymongo = "*"
flask = "*"
requests = "*"
pytest = "*"

[dev-packages]

[scripts]
format = "black ."
lint = "pylint **/*.py"
test = "pytest"

[requires]
python_version = "3.10"
